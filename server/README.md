# Create an environment
```
mkdir myproject
cd myproject
python3 -m venv .venv
```
# Activate the environment
```
. .venv/bin/activate
```

# Install dependencies
Run command `pip install -r requirements.txt` in your terminal in your project directory.

# Run server
Run command ` flask --app app run`